import {Component, OnInit, Inject} from '@angular/core';
import {Router} from '@angular/router';

@Component({
    selector: 'hot-categories',
    moduleId: module.id,
    templateUrl: 'hot-categories.template.html'
    })
export class HotCategoriesComponent {
     
    
    }